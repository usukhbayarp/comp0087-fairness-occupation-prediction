# Pythia_finetuned predictions

This folder contains predictions using finetuned pythia model.

Unmasked folder contains both full and extracted preds from a finetuned pythia model using unmasked dataset.

Masked folder contains both full and extracted preds from a finetuned pythia model using masked dataset.

The id series follows the same pattern of person 2's work. 