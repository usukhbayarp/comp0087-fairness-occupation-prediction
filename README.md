# COMP0087 Group Project

Fairness and proxy bias analysis in occupation prediction using the Bias-in-Bios dataset.

## Structure
- src/: model training, inference, evaluation
- data/: dataset loaders and preprocessing
- results/: generated tables and figures

## Setup
```bash
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt```

## Running
Instructions will be added as components are implemented.
